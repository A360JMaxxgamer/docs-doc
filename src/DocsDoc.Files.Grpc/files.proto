syntax = "proto3";
option csharp_namespace = "DocsDoc.Files.Grpc";

service UploadService {
	rpc Upload(stream UploadRequest) returns (UploadComplete);
}

message UploadRequest {
	repeated bytes data = 1;
}

message UploadComplete {
	bool success = 1;
}